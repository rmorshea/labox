# Overview

At its core Labox is a Python framework for storing heterogeneous data. However it also supplies
useful tools built atop it. For example, it includes a basic ASGI application for serving data
that was stored using Labox and a route for displaying it in a browser.
