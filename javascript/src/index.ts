/**
 * Labox JavaScript/TypeScript Library
 * 
 * Entry point for the labox library
 */

export const version = '0.1.0';

/**
 * Example function to demonstrate the library structure
 */
export function hello(name: string = 'World'): string {
  return `Hello, ${name}!`;
}

// Export other modules here as you build out the library
